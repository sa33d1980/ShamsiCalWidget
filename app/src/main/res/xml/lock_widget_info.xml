<?xml version="1.0" encoding="utf-8"?>
<!--
    Lock Screen widget — widgetCategory includes keyguard.
    Android 5+ supports lock screen widgets on some OEM ROMs (Samsung, etc.).
    On stock Android 5+, keyguard widgets were removed; this still works on
    Samsung One UI and some other custom launchers.
-->
<appwidget-provider
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="110dp"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:maxResizeWidth="250dp"
    android:maxResizeHeight="250dp"
    android:minResizeWidth="80dp"
    android:minResizeHeight="80dp"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/widget_home"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="keyguard|home_screen"
    android:description="@string/widget_lock_name"
    android:previewImage="@drawable/ic_launcher" />
